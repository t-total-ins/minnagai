<h2>マイページ</h2>
<div class="container">
  <div class="col-md-5">
    <%= render 'accountSidebar', {userTorokuSumi: @user} %>
  </div>
  <div class="col-md-7">
    <% if @user.exists? %>
      <h2>お知らせ</h2>
      <div class="list-group">
        <% @notice.each do |notice| %>
          <%= link_to(notice, class: 'list-group-item') do %>
            <h4 class="list-group-item-heading">
              <strong><%= notice.title %></strong>
            </h4>
            <p class="list-group-item-text">
              お知らせ日時：<%= notice.created_at.strftime("%Y/%m/%d %H:%M") %>
            </p>
            <p class="list-group-item-text">
              <%= truncate(notice.content, length: 70) %>
            </p>
          <% end %>
        <% end %>
        <%= paginate @notice %>
      </div>
    <% else %>
      <b><%= render plain: '会員情報を登録しなくてもプロジェクトには参加できますが、会員情報の登録をしていないと購入手続きには進めません。ご注意ください。' %></b>
    <% end %>
  </div>
</div>
